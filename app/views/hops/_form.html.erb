<%= form_with(model: hop, local: true) do |form| %>
  <% if hop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hop.errors.count, "error") %> prohibited this hop from being saved:</h2>

      <ul>
      <% hop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :hop_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :hop_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
